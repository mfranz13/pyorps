name: Build and Publish to PyPI

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build-wheels:
    name: Build wheels on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            CIBW_ARCHS_LINUX: "x86_64"
          - os: windows-latest
            CIBW_ARCHS_WINDOWS: "AMD64"

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install cibuildwheel
        run: python -m pip install cibuildwheel==2.16.2

      - name: Build wheels on Linux
        if: runner.os == 'Linux'
        env:
          # Target CPython versions
          CIBW_BUILD: "cp311-* cp312-*"
          # Skip 32-bit and musllinux if unsupported
          CIBW_SKIP: "*-win32 *-manylinux_i686 *-musllinux_i686"

          # Use newer manylinux image with GCC 12 for better C++ support
          CIBW_MANYLINUX_X86_64_IMAGE: manylinux_2_28

          # Architecture
          CIBW_ARCHS_LINUX: ${{ matrix.CIBW_ARCHS_LINUX }}

          # Build-time prerequisites for Linux
          CIBW_BEFORE_BUILD: >
            python -m pip install --upgrade pip setuptools wheel &&
            python -m pip install "numpy>=2.0.0" "cython>=3.0.0" &&
            python -m pip list

          # Ensure the build uses setuptools properly
          CIBW_ENVIRONMENT: >
            SETUPTOOLS_USE_DISTUTILS=stdlib
            SETUPTOOLS_ENABLE_FEATURES=legacy-editable

          # Do not run tests inside manylinux containers
          CIBW_TEST_SKIP: "*-manylinux_*"

          # Increase verbosity to debug build issues
          CIBW_BUILD_VERBOSITY: 2

        run: python -m cibuildwheel --output-dir wheelhouse

      - name: Build wheels on Windows
        if: runner.os == 'Windows'
        env:
          # Target CPython versions
          CIBW_BUILD: "cp311-* cp312-*"
          # Skip 32-bit
          CIBW_SKIP: "*-win32"

          # Architecture
          CIBW_ARCHS_WINDOWS: ${{ matrix.CIBW_ARCHS_WINDOWS }}

          # Build-time prerequisites for Windows - use python -m pip to avoid upgrade issues
          CIBW_BEFORE_BUILD: >
            python -m pip install --upgrade pip setuptools wheel numpy>=2.0.0 cython>=3.0.0

          # Ensure the build uses setuptools properly
          CIBW_ENVIRONMENT: >
            SETUPTOOLS_USE_DISTUTILS=stdlib
            SETUPTOOLS_ENABLE_FEATURES=legacy-editable

          # Increase verbosity to debug build issues
          CIBW_BUILD_VERBOSITY: 2

        run: python -m cibuildwheel --output-dir wheelhouse

      - name: Verify wheel contents
        shell: bash
        run: |
          echo "Checking wheel contents for Cython extensions..."
          for wheel in wheelhouse/*.whl; do
            if [ -f "$wheel" ]; then
              echo "===== Checking $wheel ====="
              python -m zipfile -l "$wheel" | grep -E "path_algorithms.*\.(pyd|so)" || echo "WARNING: path_algorithms extension not found!"
              python -m zipfile -l "$wheel" | grep -E "path_core.*\.(pyd|so)" || echo "WARNING: path_core extension not found!"
              echo ""
            fi
          done

      - name: Upload wheels
        uses: actions/upload-artifact@v4
        with:
          name: cibw-wheels-${{ matrix.os }}-${{ strategy.job-index }}
          path: ./wheelhouse/*.whl

  build-sdist:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install build dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel
          python -m pip install build "numpy>=2.0.0" "cython>=3.0.0"

      - name: Build source distribution
        run: python -m build --sdist

      - name: Upload sdist
        uses: actions/upload-artifact@v4
        with:
          name: cibw-sdist
          path: dist/*.tar.gz

  test-wheels:
    name: Test built wheels on hosts
    needs: [build-wheels, build-sdist]
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            python: "3.11"
          - os: ubuntu-latest
            python: "3.12"
          - os: windows-latest
            python: "3.11"
          - os: windows-latest
            python: "3.12"

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python }}

      - name: Download wheels
        uses: actions/download-artifact@v4
        with:
          pattern: cibw-wheels-*
          merge-multiple: true
          path: wheelhouse/

      - name: Upgrade pip
        run: python -m pip install --upgrade pip setuptools wheel

      - name: Install wheel on Linux (with full extra)
        if: runner.os == 'Linux'
        shell: bash
        run: |
          # Find the appropriate wheel for this Python version
          WHEEL=$(ls wheelhouse/pyorps*cp${PYTHON_VERSION/./}*linux*.whl 2>/dev/null | head -n1 || ls wheelhouse/pyorps*.whl | head -n1)
          echo "Installing wheel: $WHEEL"
          
          # Install the wheel with full extras
          python -m pip install "${WHEEL}[full]" --force-reinstall
          
          # Verify the installation
          python - <<'PY'
          import sys
          import pyorps
          import importlib.util
          
          print("Python version:", sys.version)
          print("pyorps imported from:", pyorps.__file__)
          
          # Check for Cython extensions
          try:
              import pyorps.utils.path_algorithms as pa
              print("path_algorithms extension imported from:", pa.__file__)
          except ImportError as e:
              print(f"ERROR: Could not import path_algorithms: {e}")
              sys.exit(1)
          
          try:
              import pyorps.utils.path_core as pc
              print("path_core extension imported from:", pc.__file__)
          except ImportError as e:
              print(f"ERROR: Could not import path_core: {e}")
              sys.exit(1)
          
          print("SUCCESS: All extensions loaded correctly!")
          PY
        env:
          PYTHON_VERSION: ${{ matrix.python }}

      - name: Install wheel on Windows (with full extra)
        if: runner.os == 'Windows'
        shell: pwsh
        env:
          PYTHONIOENCODING: utf-8
          PYTHONUTF8: 1
          PYTHON_VERSION: ${{ matrix.python }}
        run: |
          # Find the appropriate wheel for this Python version
          $pyver = $env:PYTHON_VERSION -replace '\.',''
          $wheel = Get-ChildItem wheelhouse\pyorps*cp${pyver}*win*.whl -ErrorAction SilentlyContinue | Select-Object -First 1
          if (-not $wheel) {
            $wheel = Get-ChildItem wheelhouse\pyorps*.whl | Select-Object -First 1
          }
          Write-Host "Installing wheel: $wheel"
          
          # Install the wheel with full extras
          python -m pip install "$wheel[full]" --force-reinstall
          
          # Verify the installation
          python -c @"
          import sys
          import pyorps
          import importlib.util
          
          print('Python version:', sys.version)
          print('pyorps imported from:', pyorps.__file__)
          
          # Check for Cython extensions
          try:
              import pyorps.utils.path_algorithms as pa
              print('path_algorithms extension imported from:', pa.__file__)
          except ImportError as e:
              print(f'ERROR: Could not import path_algorithms: {e}')
              sys.exit(1)
          
          try:
              import pyorps.utils.path_core as pc
              print('path_core extension imported from:', pc.__file__)
          except ImportError as e:
              print(f'ERROR: Could not import path_core: {e}')
              sys.exit(1)
          
          print('SUCCESS: All extensions loaded correctly!')
          "@

      - name: Install pytest
        run: python -m pip install pytest

      - name: Run full tests on Linux from a temp directory
        if: runner.os == 'Linux'
        shell: bash
        run: |
          TMPDIR=$(mktemp -d)
          cp -r tests "$TMPDIR"/tests_run
          cd "$TMPDIR"/tests_run
          python -m pytest . --tb=short --import-mode=importlib

      - name: Run full tests on Windows 3.11 from a temp directory
        if: runner.os == 'Windows' && matrix.python == '3.11'
        shell: pwsh
        env:
          PYTHONIOENCODING: utf-8
          PYTHONUTF8: 1
        run: |
          $tmp = Join-Path $env:TEMP "tests_run_ci"
          New-Item -ItemType Directory -Path $tmp -Force | Out-Null
          Copy-Item -Recurse -Force tests (Join-Path $tmp "tests_run")
          Set-Location (Join-Path $tmp "tests_run")
          python -m pytest . --tb=short --import-mode=importlib

      - name: Smoke test on Windows 3.12 (avoid LLVM teardown assertion)
        if: runner.os == 'Windows' && matrix.python == '3.12'
        shell: pwsh
        env:
          PYTHONIOENCODING: utf-8
          PYTHONUTF8: 1
        run: |
          python -c @"
          import pyorps
          import pyorps.utils.path_algorithms as m
          import pyorps.utils.path_core as c
          print('Imported pyorps:', pyorps.__file__)
          print('Imported path_algorithms extension:', m.__file__)
          print('Imported path_core extension:', c.__file__)
          print('SUCCESS: All modules imported correctly!')
          "@

  publish:
    name: Publish to PyPI
    needs: [build-wheels, build-sdist, test-wheels]
    runs-on: ubuntu-latest
    environment: pypi
    permissions:
      id-token: write
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: cibw-*
          merge-multiple: true
          path: dist/

      - name: List all downloaded files
        run: |
          echo "Contents of dist/:"
          ls -la dist/

      - name: Collect distributions
        shell: bash
        run: |
          mkdir -p upload
          # Move wheels and source distributions
          find dist -name "*.whl" -exec mv {} upload/ \; 2>/dev/null || true
          find dist -name "*.tar.gz" -exec mv {} upload/ \; 2>/dev/null || true
          echo "Files to be uploaded:"
          ls -la upload/

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: upload/
