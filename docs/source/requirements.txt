# Minimal documentation dependencies for fast RTD builds
sphinx>=8.0.0
sphinx-rtd-theme>=2.0.0
myst-parser>=2.0.0

# Only the absolute minimum to import pyorps
# Everything else will be mocked
numpy>=2.0.0
